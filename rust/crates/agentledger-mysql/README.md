# agentledger-mysql

Rust MySQL adapter boundary for AgentLedger Runtime.

```toml
agentledger-mysql = "1.2"
```

This crate re-exports the dependency-light injected-client MySQL adapter contract. Runtime core does not bundle a MySQL driver.
