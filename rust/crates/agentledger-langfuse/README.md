# agentledger-langfuse

Langfuse evidence and trace export adapter boundary for AgentLedger Rust.

```rust
use agentledger_langfuse::PACKAGE_NAME;
```

The crate is intentionally dependency-light and re-exports the runtime Langfuse adapter boundary.
