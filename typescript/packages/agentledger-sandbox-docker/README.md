# agentledger-sandbox-docker

TypeScript Docker sandbox adapter boundary for AgentLedger Runtime.

```bash
npm install agentledger-runtime agentledger-sandbox-docker
```

```js
import { DockerSandboxAdapter } from 'agentledger-sandbox-docker';
```

